{
  "name": "mbp-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve",
    "admin:dev": "npm --prefix admin run dev",
    "admin:build": "npm --prefix admin run build",
    "admin:start": "npm --prefix admin run start",
    "test": "echo \"No test specified\""
  },
  "dependencies": {
    "mysql2": "^3.9.0"
  }
}